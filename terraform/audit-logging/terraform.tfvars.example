# Copy to terraform.tfvars and edit. terraform.tfvars is .gitignored.
#
# Add a hostname here for each managed Mac that should be given write
# credentials. Must match `hostname -s` on the machine.
writer_hostnames = [
  # "alice-mbp",
  # "bob-mbp",
]

# Map of label -> IAM user name for read-only investigation users.
reader_principals = {
  # "max" = "claude-audit-reader-max"
}
